name: macOS Compile Smoke

on:
  workflow_dispatch:

# Cancel stale PR runs on new pushes; let push builds on develop/main complete
concurrency:
  group: darwin-smoke-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

permissions:
  contents: read

jobs:
  darwin-smoke:
    name: macOS Compile Smoke
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7
        with:
          go-version-file: go.mod

      - name: Compile smoke
        run: go test -run '^$' ./...

      # Native darwin runtime coverage for background command execution
      # (#346): process-group lifecycle, manager suites, and the REPL
      # shutdown paths that kill and reap real process groups.
      - name: Background exec runtime
        run: go test -race -run 'TestBackground(Process|Manager)|TestRunBackground|TestJobs' ./agent/tools ./cmd/golem
