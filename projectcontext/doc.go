// Package projectcontext discovers and safely reads durable project-context
// files such as AGENTS.md. Each document includes bounded prompt content and
// full-file evidence so consumers can make their own trust decisions.
package projectcontext
