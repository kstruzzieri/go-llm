- Create a detailed implementation strategy plan that specialized agents can run in parallel, where appropriate.
- Explicitly state that no hard-coded/fallback/mock/fake data of any kind should be coded. All data/values should be
dynamically derived from services/database/calculations, and if data is not available then proper error/message handling.
- After each phase perform a code review, then fix all items found, then code review again and repeat until no issues found.