{
  "trace_id": "r3c-type-semantics-03",
  "oracles": [
    "The standard encoding/json package ignores unexported struct fields even when they have tags.",
    "`apiKey` cannot round-trip through Save and Load unless it is exported or custom marshal/unmarshal code handles it."
  ],
  "non_trivial": true,
  "solvable_from_context": true,
  "screened_by": "opus-selfsolve",
  "notes": "Designed to catch over-reliance on JSON tags without checking export visibility."
}
