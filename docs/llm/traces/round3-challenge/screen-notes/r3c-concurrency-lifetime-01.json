{
  "trace_id": "r3c-concurrency-lifetime-01",
  "oracles": [
    "The goroutines use context.Background instead of the derived context, so neither caller cancellation nor sibling-failure cancellation reaches fetchOne.",
    "The loop variable is intentionally shadowed correctly; treating it as the main issue is an expected wrong path."
  ],
  "non_trivial": true,
  "solvable_from_context": true,
  "screened_by": "opus-selfsolve",
  "notes": "Designed to test cancellation propagation rather than generic goroutine cleanup advice."
}
